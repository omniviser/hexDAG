apiVersion: hexdag/v1
kind: Pipeline
metadata:
  name: deep-research-agent-staging
  description: Deep research agent with web search capabilities (STAGING)
spec:
  ports:
    llm:
      adapter: open_ai_adapter
      config:
        api_key: ${STAGING_OPENAI_API_KEY}
        model: gpt-4o-mini
        temperature: 0.7
    tool_router:
      adapter: tool_router
      config:
        tools: {}
  nodes:
  - kind: macro_invocation
    metadata:
      name: research_agent
    spec:
      macro: core:reasoning_agent
      config:
        main_prompt: 'You are a deep research agent. Research the following question
          thoroughly: {{research_question}}'
        max_steps: 5
        allowed_tools:
        - search
        - calculate
        tool_format: mixed
    dependencies: []
