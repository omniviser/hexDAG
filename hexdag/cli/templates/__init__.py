"""Bundled template assets for ``hexdag`` scaffolding commands.

This subpackage ships non-Python data files (e.g. the Claude Code ``.claude/`` assets under
``claude/``) inside the wheel so ``hexdag init`` can copy them into a target project via
``importlib.resources``.
"""
